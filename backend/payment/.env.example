# Copy to .env and adjust for your environment.
# Backend settings read these via python-dotenv (see payment/settings.py).

DJANGO_SECRET_KEY=your-secret-key-here
DJANGO_DEBUG=True
DJANGO_ALLOWED_HOSTS=*